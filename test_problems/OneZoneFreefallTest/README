### OneZoneFreefallTest ###

To create the figure in the paper of the density vs. temperature tracks 
for different metallicities, the OneZoneFreefallTest.enzo parameter file 
should be run with five different configurations.  You will also need the 
cooling data file, solar_2008_3D_metals.h5, from the input directory in the 
enzo source.

For each, the following 
parameters need to be changed:

OneZoneFreefallTestMinimumMetallicity
OneZoneFreefallTestMaximumMetallicity
GlobalDir

For the five runs, the values should be
OneZoneFreefallTestMinimumMetallicity OneZoneFreefallTestMaximumMetallicity GlobalDir
0    0    mf
1e-5 1e-5 Z-5
1e-4 1e-4 Z-4
1e-3 1e-3 Z-3
1e-2 1e-2 Z-2

When these have been run, the figure can be made by running:
python plot.py

This requires that yt is installed.